server:
  port: 9192
spring:
  application:
    name: ORDER-SERVICE
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  zipkin:
    base-url: http://localhost:9411/
logging:
  file: D:\logs\microservice.log